export default class Calculator {
  add(
    a: number,
    b: number
  ) {
    return a + b;
  }
}

export const PI = 3.14;

export function square(
  value: number
): number {
  return value * value;
}